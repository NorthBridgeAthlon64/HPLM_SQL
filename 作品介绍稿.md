# 硬件产品全生命周期管理系统（HPLM）—— 作品介绍稿

**时长**：约 5 分钟（含录屏演示概况）　**演讲人**：赵晟闻

---

各位老师好，我是赵晟闻。今天我介绍的作品是**硬件产品全生命周期管理系统**，简称 HPLM。

---

## 一、选题背景（约 40 秒）

这个选题来源于我自己做硬件开发时的真实痛点。大一打比赛做 PCB 设计时，我遇到了几个很实际的问题：

第一是**元器件管理**。几十种电阻、电容、IC 分散在不同的采购订单里，全靠 Excel 和记忆管理，经常出现"以为有库存，焊接时才发现缺料"的情况。

第二是**版本管理混乱**。硬件产品的迭代不像软件有 Git 可以管理，v1.0 用到了哪些元器件、v2.0 改了什么，全靠原理图注释——时间一长根本查不到。

第三是**故障溯源困难**。产品出货后出了问题，只能靠工程师经验推测是哪个批次、哪个元器件的问题，缺乏系统化的追溯链路。

所以我想做一个系统，把元器件选型、BOM 配置、生产批次、测试记录、售后维修到客户评价的全链路打通。

---

## 二、系统架构（约 50 秒）

整个系统分为三个层次：

**数据层**是 PostgreSQL 数据库，设计了 18 张表、28 条关系，满足 3NF。核心实体包括元器件、产品版本、BOM 清单、生产批次、维修工单等。多对多关系（如元器件和供应商）通过关联表正确分解。数据字典为每张表的 5 列属性做了完整定义。

**服务层**包含 6 个 Service 和 18 个 Repository，采用 Repository 模式统一管理数据访问。重点 Service 包括库存入库事务（流水记录、成本计算、库存更新在一个事务内完成）、生产批次创建（BOM 自动生成投料清单并扣减库存）、故障产品溯源。

**界面层**基于 Qt6 C++ 开发，包含 8 个功能页面。实际编译运行通过，支持 PostgreSQL 14+，代码总量约 74 个源文件。

---

## 三、核心业务流程（约 70 秒）

我重点介绍三个核心流程：

**BOM 物料清单配置**：工程师进入产品版本页面，左侧是产品树，右侧是 BOM 表格。可以从元器件库搜索添加元器件、修改用量、配置 PCB 位号。系统自动根据元器件最新采购价计算单台预估物料成本。

**元器件采购入库**：采购人员选择供应商和元器件，输入数量和单价。系统在事务内完成库存流水记录、更新元器件采购价格、更新仓库库存、按移动平均法重新计算库存成本。如果库存超出仓库容量上限，系统会拒绝入库。

**故障产品溯源**：这是我认为最有价值的功能。质量工程师输入维修单号或批次号，系统自动联查六张表——维修工单、生产批次、投料清单、测试记录、元器件主表和供应商表——生成完整的溯源报告，包括"这个故障产品是哪个批次生产的、用了哪些元器件、供应商是谁、出厂前通过了哪些测试"。

---

## 四、文档与设计（约 40 秒）

在文档层面，我完成了四份文档的撰写：

01 功能说明文档包含应用场景描述、8 项功能列表、6 张 Mermaid 流程图，每张流程图都配有详细的文字描述。

02 数据库设计文档采用 ER 图、关系模式、数据字典、DDL 脚本的三级递进结构。18 张表都有 5 列数据字典，数据类型选择合理，金额用 DECIMAL、日期用 DATE，所有约束定义完整。

03 功能设计文档包含 8 个经过编号的原型界面，界面-数据库交互映射表有 45 行操作记录。

04 AI 使用记录文档记录了 29 次交互，每次都有具体的提问上下文和我的独立思考与整合。

---

## 五、BOM 导入与导出功能（约 40 秒）

针对导入效率低的问题，我实现了 BOM 文件的批量导入导出功能。支持嘉立创 EDA 导出的 CSV 文件，Tab 分隔、10 列固定格式。导入时系统逐行匹配已有元器件、自动创建缺失元器件和供应商，支持三色标记预览。导出功能将系统内的 BOM 清单逆向生成为嘉立创格式，实现导入、编辑、导出、再导入的闭环。

---

## 六、系统录屏演示（约 60 秒）

作品同步提交了 5 分钟以内的系统录屏，内容覆盖 6 项核心功能，并采用"分屏验证"方式证明数据真实存储在数据库中。

录屏分为三部分：**数据库结构展示**——在 DBeaver 中展示 18 张表的列名、类型、主外键约束，展示 28 条关系在数据库层面的体现。**功能操作演示**——依次展示登录验证、元器件管理（搜索/新增/编辑库存）、产品版本与 BOM 配置、生产批次创建、维修工单创建与换料、故障产品溯源（六表联查 HTML 报告）。**数据关联验证**——每项功能操作结束后，切换到 DBeaver 按 F5 刷新对应表，证明新数据已写入 PostgreSQL 而非硬编码，比如新增元器件后 `Component` 表多了一行、创建批次后 `ProductionBatch` 和 `BatchMaterial` 两张表同时出现新记录。

分屏方案采用左侧 Qt 界面操作、右侧 DBeaver 验证的双窗口布局，提前准备好了 `SQL_Monitor.sql` 中的 12 条监控查询（覆盖元器件/BOM/批次/投料/维修/溯源/供应商/评价），录屏时只需按 F5 即可看到实时变化。录屏工具使用 Xbox Game Bar（Win+Alt+R），分辨率 1920×1080、H.264 编码、码率约 3Mbps 控制在 100MB 以内。

---

## 七、AI 使用心得（约 20 秒）

在整个开发过程中，我的角色经历了三个阶段：初期让 AI 生成框架我来复核；中期自己发现问题并指挥 AI 修复；后期自己定义架构、做风险评估、提出功能需求。AI 辅助的是执行，但架构设计、业务需求、风险判断始终是我自己的思考。

另外，整个项目的版本管理也使用了 git，采用 Qt6App 开发分支加 master 稳定分支的策略，累计 30 多次 commit，每次都有清晰的分类前缀和描述。这虽然不是一个技术亮点，但体现了规范化的工程习惯。

---

## 八、结语（约 20 秒）

这个项目的完成，让我把数据建模、关系模式设计、事务处理这些课内知识真正用到了一个有真实硬件开发场景的问题中。作品还有很多不完善的地方，但我认为它已经具备了从"课程作业"进化为"实用工具"的基础。

谢谢老师！
